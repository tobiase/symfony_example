{% extends 'base.html.twig' %}

{% block title %}Stoff{% endblock %}

{% block body %}
    <h1>Stoff</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stoff.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stoff.name }}</td>
            </tr>
            <tr>
                <th>Beschreibung</th>
                <td>{{ stoff.beschreibung }}</td>
            </tr>
            <tr>
                <th>Preis</th>
                <td>{{ stoff.preis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stoff_index') }}">back to list</a>

    <a href="{{ path('stoff_edit', {'id': stoff.id}) }}">edit</a>

    {{ include('stoff/_delete_form.html.twig') }}
{% endblock %}